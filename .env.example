# Below are the default settings of docker container for development
BASE_URL="http://localhost:8080/"
CI_ENV=development
DB_HOSTNAME=mysql
DB_DATABASE=kakeibou-vn
DB_USERNAME=root
DB_PASSWORD=root

# Only log ERROR messages on production
LOG_THRESHOLD=1
